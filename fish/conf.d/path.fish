# Add $HOME/bin to path
set -gx PATH $HOME/bin $PATH
set -gx PATH /home/csesmau/go/bin $PATH
set -gx PATH /home/linuxbrew/.linuxbrew/opt/rustup $PATH

# Add work-scripts bin directory to path
set -gx PATH /home/csesmau/work-scripts/bin $PATH
